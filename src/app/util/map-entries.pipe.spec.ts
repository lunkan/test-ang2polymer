import { MapEntitiesPipe } from './map-entities.pipe';

describe('MapEntitiesPipe', () => {
  it('create an instance', () => {
    const pipe = new MapEntitiesPipe();
    expect(pipe).toBeTruthy();
  });
});
