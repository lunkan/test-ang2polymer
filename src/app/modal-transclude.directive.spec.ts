import { ModalTranscludeDirective } from './modal-transclude.directive';

describe('ModalTranscludeDirective', () => {
  it('should create an instance', () => {
    const directive = new ModalTranscludeDirective();
    expect(directive).toBeTruthy();
  });
});
